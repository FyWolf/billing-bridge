/.idea
/.vscode
.DS_Store

# The panel writes plugin status back into plugin.json at install/enable time.
# Those local edits should not travel with the repo.
plugin.json.bak
CLAUDE.md

# graft's local graph cache — regenerable, not committed (run `graft build`).
graft/
